<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel = "stylesheet" href = "styles/style.css">
    <title>Entries</title>
</head>
<body>
    <div id = "main">
        <h1>All Posts</h1>
        <% if (posts.length === 0) { %>
            <p id = "noPost">No posts found, click on "Create a New Post" to make your first post</p>
            <hr>
        <% } else { %>
            <% for (let i = 0; i < posts.length; i++) { %>
                <p>Author: <%= posts[i].author %></p>
                <p>Title: <%= posts[i].title %></p>
                <p>Content: <%= posts[i].content %></p>
                <p>Time Created: <%= posts[i].create_at %></p>
                <hr>
            <% } %>
        <% } %>
        <a href = "/" class = "button">Create a New Post</a>
    </div>
</body>
</html>